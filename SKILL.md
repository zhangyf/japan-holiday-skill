---
name: japan-holiday
description: 查询指定日期段的日本祝日（公共假期）并进行连休分析，判断日本人是否会请假连休。触发词：日本假期、日本休日、日本祝日、日本公休日、Japan holiday、日本连休、日本GW。
agent_created: true
---

# 日本祝日・連休分析

当用户发来日期段并带有"日本假期""日本休日""日本祝日""日本公休日""Japan holiday""日本连休""日本GW""日本黄金周"等关键词时，执行以下流程。

## 触发关键词

以下任一关键词出现时激活本技能：
- 日本假期 / 日本休日 / 日本公休日 / 日本祝日
- Japan holiday
- 日本连休 / 日本GW / 日本黄金周 / 日本大型連休

## 处理流程

### Step 1: 解析日期范围

用户输入示例 → 解析结果：
- `2026年9月` → 2026-09-01 到 2026-09-30
- `9月` → 当前年份的 9 月
- `2026-09` → 同上
- `2026年9月到10月` → 2026-09-01 到 2026-10-31
- `9/1-9/30` → 当月 9/1 到 9/30
- `2026年` → 全年
- 没指定年份 → 默认当年

### Step 2: 读取配置

读取 `~/.workbuddy/skills/japan-holiday/config.json` 获取 API Key 和端点 URL。

### Step 3: 获取祝日数据（三源降级）

**主源：Calendarific API** ✅ 已验证可用

用 WebFetch 调用：
```
https://calendarific.com/api/v2/holidays?api_key={KEY}&country=JP&year={YEAR}&type=national
```

从响应的 `response.holidays[]` 中提取每个 holiday：
- `name` → 祝日名（英文）
- `date.iso` → 日期
- `date.datetime.day` → 日
- `date.datetime.month` → 月
- `date.datetime.year` → 年

**备源1：WebFetch bestcalendar.jp**

Calendarific 失败时，用 WebFetch 抓取：
```
https://bestcalendar.jp/{YEAR}/2/holidays
```

页面包含全年祝日表格，提取祝日名称和日期。格式为日文（例：`1月1日(木) 元日`），需解析为日期。

**备源2：Japanese Holiday API** ⚠️ 可能过期

```
https://api.janmir.me/holiday/list?format=M/dd,W,D
```

数据可能不是当前年份，仅作最后兜底。返回格式：`"Jan/01,Mon,New Year's Day"`。

解析规则：`{月缩写}/{日},{星期缩写},{祝日名}`
月缩写映射：Jan→1, Feb→2, Mar→3, Apr→4, May→5, Jun→6, Jul→7, Aug→8, Sep→9, Oct→10, Nov→11, Dec→12

**三个都失败** → 提示用户手动查 https://bestcalendar.jp 。

### Step 4: 过滤日期范围

从全年数据中筛选出落在用户指定日期范围内的祝日。

### Step 5: 连休分析

对日期范围内的每个日期，标记：
- 是否为祝日
- 是否为周末（周六/周日）
- 是否为振替休日（仅 Calendarific 可能提供此信息）

然后执行以下分析：

#### 5a. 3連休以上检测
- 扫描日历，找到连续的「祝日 or 周末」块
- 块长度 ≥ 3 → 标记为 N連休
- 输出：期间、天数、包含的祝日

#### 5b. 飛び石連休检测
- 判断逻辑：祝日与周末之间只隔 1 个工作日
- 例如：周五祝日 → 周六日自然连成 3 連休
- 例如：周二祝日 → 周一请有給就能 4 連休
- 标注"日本人通常会请假凑成 X 連休"或"已经自然形成 3 連休"

#### 5c. 大型連休识别
- **GW（ゴールデンウィーク）**：4/29 昭和の日 〜 5/5 こどもの日 前后
- **お盆**：8/13〜8/15（非法定祝日但普遍休暇，需特别标注）
- **SW（シルバーウィーク）**：9月中旬敬老の日 + 秋分の日 连休
- **年末年始**：12/29〜1/3（非法定但普遍休業，标注为"実質的な年末年始休暇"）

#### 5d. 振替休日・国民の休日
- 振替休日：祝日落在周日 → 周一补休
- 国民の休日：两个祝日之间夹的工作日（只在祝日に挟まれた平日に発生）

### Step 6: 输出可视化日历

**必须先调用 `read_me` 加载 `chart,diagram` 模块，再用 `show_widget` 输出日历 SVG。**

日历 SVG 规范（视觉约束）：

1. **布局**：viewBox="0 0 680 560"，7 列等宽（各 90px），左 margin 25px，列间距 0
2. **对齐**：星期标题行（月火水木金土日）与下方日期格严格列对齐，标题文字居中于列中心
3. **格子**：宽 90px × 高 68px，直角（rx=4），间距 2px
4. **色彩区分（浅色主题）**：
   - 法定假日：`#FDE8E8` 背景，`#C0392B` 文字，日期加粗
   - 国民の休日（祝日に挟まれた平日）：`#FFF3E0` 背景，`#E67E22` 文字
   - 连休周末（連休に含まれる土日）：`#FEF0EC` 背景，`#C0392B` 文字，标注"连休周末"
   - 普通周末（土日统一）：`#F3F4F6` 背景，`#9CA3AF` 文字
   - 请假即連休（有給1日で連休化）：`#FFFDE7` 背景 + `#F39C12` 虚线边框（stroke-dasharray="3,3"）
   - 普通工作日：`#F8F9FA` 背景，`#374151` 文字
5. **格子内容**：上方居中日期数字（font-size:16,bold），下方居中标签（font-size:10），标签用中文
6. **图例**：标题下方水平排列 6 个色块说明，中文标签
7. **下方文字总结**：日历图下方用文字列出核心休假结论（日期 + 連休天数 + 是否需请假）
8. **禁止日文标签出现在格子内**：格子内只用中文（"法定假日""自动连休""请假即連休""周末"），不用"祝日""有給"等日文

### Step 7: 输出文字分析

日历图下方文字总结：

```
### 核心结论

| 期間 | 天数 | 类型 | 说明 |
|------|------|------|------|
| 9/19(土)〜9/23(水) | 5連休 | 自动连休 | 法定假日+国民休息日，无需请假 |
| 9/18(金)请假 → 9/18〜9/23 | 6連休 | 请假即连休 | 请1天年假即可 |

### 业务影响
- {不适合商务联络的时段}
- {最早恢复日期}
```

### 祝日名中日对照参考

常见祝日的中文翻译：
- 元日 → 元旦
- 成人の日 → 成人节
- 建国記念の日 → 建国纪念日
- 天皇誕生日 → 天皇诞生日
- 春分の日 → 春分节
- 昭和の日 → 昭和日
- 憲法記念日 → 宪法纪念日
- みどりの日 → 绿之日
- こどもの日 → 儿童节
- 海の日 → 海之日
- 山の日 → 山之日
- 敬老の日 → 敬老日
- 秋分の日 → 秋分节
- スポーツの日 → 体育日
- 文化の日 → 文化日
- 勤労感謝の日 → 勤劳感谢日
- 振替休日 → 补休日
- 国民の休日 → 国民休息日

### 日本祝日知识（输出时酌情引用）

- 日本每年有 16 个法定祝日（国民の祝日），约 18 天（含振替休日）
- 祝日落在周日时，次日（周一）为振替休日；落在周六不补
- 两个祝日之间只隔一个工作日时，该工作日自动成为「国民の休日」
- GW（ゴールデンウィーク）是 4 月底到 5 月初的集中连休，日本人出行高峰
- お盆（8/13-15）非祝日但大多数公司給休暇，是仅次于 GW 和年末年始的出行高峰
- 年末年始（12/29-1/3）政府部门和企业普遍休業
- 日本人年均带薪休假取得率约 50-60%，大型連休前后请假凑连休的人较多

### 关于「実質的な休暇」

以下时段不是法定祝日，但日本人普遍休假，务必在报告中标注：
- **お盆**：8/13〜8/15（关东 7/13-15 的地区差异大，报告默认标注 8/13-15）
- **年末年始**：12/29〜1/3
- 标注为 `（実質的な休暇、祝日ではない）`
